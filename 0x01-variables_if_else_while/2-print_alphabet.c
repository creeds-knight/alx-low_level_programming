#include <stdio.h>
/**
 * main - A program that prints the alphatbet in lowercase
 * Return: 0 (Success)
 */
int main(void)
{
	char m;

	m = 'a';
	while
		(m <= 'z') {
			putchar(m);
			m++;
		}
	putchar('\n');
	return (0);
}
