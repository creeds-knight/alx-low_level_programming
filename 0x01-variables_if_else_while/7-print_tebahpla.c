#include <stdio.h>
/**
 * main - A program that prins the reverse alphabet
 * Return: (0) (success)
 */
int main(void)
{
	char a;

	a = 'z';
	while
		(a >= 'a') {
			putchar(a);
			a--;
		}
	putchar('\n');
	return (0);
}
